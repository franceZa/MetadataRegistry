catalog: "{env}_catalog"
table: "{catalog}.{layer}_{source}.{dataset}"
landing: "/Volumes/{catalog}/landing_{source}/files/{dataset}/"
quarantine: "{catalog}.quarantine_{source}.{dataset}"
vault: "{catalog}.vault_{source}.{dataset}"
checkpoint: "/Volumes/{catalog}/ops/checkpoints/{layer}_{source}/{dataset}/"
run_log: "{catalog}.ops.run_log"
config_id: "{layer}.{source}.{dataset}"
contract_id: "{source}.{dataset}"
